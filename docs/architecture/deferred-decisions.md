# Deferred Decisions

| Decision                         | Why deferred                                                                                                                                                                                         | Revisit trigger                                                                           |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Database redesign                | Would invalidate parity and expand risk; product gaps catalogued as G-01…G-32 ([gaps-and-solutions.md](../frontend/gaps-and-solutions.md)); schema owned by backend ([ADR-014](decisions/ADR-014-product-data-ownership.md)). Engine is PostgreSQL 18.x; NestJS 11.x + Prisma 7.x are **DECIDED** ([ADR-015](decisions/ADR-015-backend-stack.md)). | Backend program executes [remediation-roadmap.md](../backend/remediation-roadmap.md) / [data-contract.md](../backend/data-contract.md) |
| RLS/schema changes               | Existing data contract frozen for Frontend Migration ([ADR-011](decisions/ADR-011-rls-storage-strategy.md) Phase 1)                                                                                  | Verified security defect with separate approval; or Phase 2 Backend cutover               |
| Microservices/event architecture | No migration requirement                                                                                                                                                                             | Scale or reliability evidence                                                             |
| Payment implementation           | Current billing UI is placeholder (G-07); billing webhook is sole intended writer of `profiles.plan`                                                                                                 | Product approval + provider selection; then Phase 5 of [remediation-roadmap.md](../backend/remediation-roadmap.md) |
| Email delivery provider          | **ACCEPTED RISK:** templates preserved; ship provider-agnostic adapter stubs; no real send until provider chosen                                                                                     | Provider/cost/compliance choice; swap stub for real transport                             |
| SMS provider                     | **ACCEPTED RISK:** channel and message content preserved; stub fails explicitly until configured                                                                                                     | Provider and compliance decision                                                          |
| Internationalization             | Current app is English-only                                                                                                                                                                          | Product localization requirement                                                          |
| Durable job platform             | **Ownership DECIDED** ([ADR-013](decisions/ADR-013-campaign-messaging-runtime.md)): workers outside Next.js; **product still deferred** by workload                                                  | Workload/ops evidence; choose queue vendor without Lovable transport                      |
| Portable observability vendor    | Lovable `window.__lovableEvents` **removed** (slice 14); replacement vendor not required for architecture                                                                                            | Production observability needs after Vercel cutover                                       |
| Asset vendoring (Lovable/CDN)    | **DONE (slice 2):** local binaries in `src/assets/`; OG in `public/og-image.png`; manifests removed; `npm run scan:assets` ([10-styling-and-assets.md](../frontend/10-styling-and-assets.md))        | Revisit only if a new marketing asset is introduced via an external CDN                   |
| Vercel env confirm               | **ACCEPTED RISK:** inventory documented ([env.md](../deployment/env.md)); **remember** ΓÇö set/confirm Dev / Preview / Production in Vercel UI before Next deploy; never paste secrets into git/chat | Values confirmed in Vercel UI for the Next project                                        |
| Cookie/SSR session proof (D-28)  | **ACCEPTED RISK:** migration may start; remains **BLOCKED** until NestJS JWT HTTP-only cookies are proven in Next proxy + RSC ([ADR-005](decisions/ADR-005-authentication.md) Option C). `@supabase/ssr` spike is **superseded**. | Nest test user + Next cookie/SSR harness PASS (not Supabase Auth) |
| Differentiated in-app roles      | **Locked names:** `admin` (buyer `/app`), `staff` (`/app`, **same permissions as `admin` for now**), `customer` (customer login). Product MVP (Ship 1) Redemption: any existing Staff or Admin may **scan/verify**, Shop-scoped. Staff subtypes and a later permission split are not locked. ([ADR-005](decisions/ADR-005-authentication.md), [locked role matrix](../frontend/11-authentication-migration.md#locked-role-matrix), [redemption §11](../product/reward-redemption-flow.md#11-staff-authorization-shop-level)) | Product approval to split `staff` permissions; backend owns schema |
| Shop-customer register / login   | **DECIDED as product intent:** shop customers will register/login to store their data and support **calculated KPIs**; not only owner manual add. **Passwordless:** register, login, and recovery never use a password; lost access = new OTP (SMS/WhatsApp). Routes, identity schema, and KPI list **not locked**. Working design journey (2026-08-16): one OTP funnel; first-shop link + profile setup screens; in-store returning check-in stays without OTP ([customer-portal-journey.md](../product/customer-portal-journey.md)). Not `admin` / `staff` `/app` auth ([G-33](../frontend/gaps-and-solutions.md#g-33--shop-customers-have-no-registerlogin-kpis-rely-on-owner-typed-rows), [credential recovery](../frontend/11-authentication-migration.md#credential-recovery-decided)) | Backend program + route map approval; do not add tables from this frontend repo |
| Admin creates admin/staff        | **DECIDED:** `admin` form creates `admin` or `staff`, random temp password, email with added + email + password; **first login must change the password**. After the account exists, forgotten password is the owner `/auth/forgot-password` flow; admin may re-issue a temp password. Route and extra profile fields not locked ([G-34](../frontend/gaps-and-solutions.md#g-34--admin-cannot-create-adminstaff-with-emailed-temp-password), [credential recovery](../frontend/11-authentication-migration.md#credential-recovery-decided)) | Backend create-user + messaging contracts + first-login password gate |
| Independent programs (one ACTIVE) | **DECIDED (2026-08-18, [ADR-016](decisions/ADR-016-independent-programs.md)):** many independent `loyalty_programs` per Shop; **at most one** `status = 'active'` (default). Counter QR + referral links resolve only to ACTIVE. Customer locked to `enrolled_program` until deferred POS migration (target redeemed **or** program expired). Previous ACTIVE → `archived` (not disable). Catalog / wallet / ledger program-scoped. **Supersedes** 2026-08-17 Shop-capability lock (`UNIQUE (owner_id, program_type)`, one membership per Shop). ([program-model.md](../product/program-model.md), [counter QR](../product/counter-qr-and-program-membership.md), [G-35](../frontend/gaps-and-solutions.md#g-35--shop-loyalty-is-one-row-not-independent-programs)) | Partial unique one ACTIVE per `owner_id`; memberships table; do not add tables from this frontend repo |
| Customer wallet | **DECIDED:** one **current** program card per Shop membership (type sections only if that program has them) + **Archived History** for locked legacy programs. Never mix balances across Shops. Spendable / **available** = total − pending reserved. Portal URL not locked ([program-model.md](../product/program-model.md#4-customer-membership-and-wallet), [customer-reward-progress.md](../product/customer-reward-progress.md)) | Backend `GET /api/me/wallet`; do not add tables from this frontend repo |
| Tier metric (PM-08) | **DECIDED:** ladder reads `period_points_earned` only (current period). Must **not** read `spendable_points`. Redeem/voucher does not decrement the period counter. `tier_reset_period` job zeros `period_points_earned` + displayed milestone. Tiers are time-bound **milestones** (one-time payouts), not standing VIP. Downgrade protection / grace **out of scope**. ([program-model.md](../product/program-model.md#pm-08--tier-metric-two-counters)) | Backend period counter + `roll-tier-period` job |
| Referral points kind when Points is off (PM-07) | **DECIDED:** if `is_points_enabled = false` (ACTIVE program is not `points`), `referral_reward_type` must be `voucher` for referrer and referee. `points` → HTTP 400 `REFERRAL_POINTS_REQUIRES_POINTS_ENABLED`. ([program-model.md](../product/program-model.md#pm-07--referral-reward-type-when-points-is-off)) | Enforce on `referral_settings` PATCH and on activate-non-points |
| Tier downgrade if activity drops | **Rejected / not applicable.** Milestone model + PM-08: `period_points_earned` never falls mid-period. `tierDowngradeProtection` is not a product flag. | — |
| Signup Bonus vs Referral Bonus | **DECIDED:** Signup fires on **each new program enrollment** (including POS auto-migrate into a new ACTIVE), if that program’s bonus is enabled — not once per Shop lifetime. Referral: valid `?ref=` + OTP; can **stack** with Signup on that enroll. Welcome / link-Shop screens are UX only. Referrer grant still waits for first paid invoice. ([Signup vs Referral](../frontend/loyalty-page.md#signup-bonus-vs-referral-bonus-decided)) | Backend enroll + ledger `signup_bonus` |
| Catalog redemption lifecycle | **DECIDED (Product MVP (Ship 1)):** Redeem → Available check → `pending` + reserve + `reward_snapshot` + 10-min QR → atomic staff scan `completed` or job `expired`. Scan is verification. Create idempotent. Digital may instant-complete. **Do not implement** staff Approve/Reject. **Deferred:** general refund / reversal (out of Product MVP (Ship 1)); emergency force-delete cancel+refund is specified but later-phase. ([reward-redemption-flow.md](../product/reward-redemption-flow.md), [G-20](../frontend/gaps-and-solutions.md#g-20--rewardsredeemed_count-vs-earn)) | Backend redeem APIs + expiry worker |
| Pending redemption vs later disable / price / expiry | **DECIDED (2026-08-18):** (1) **§14.1** `reward_snapshot` at create; program/reward edits **prospective only**; material catalog cuts = new reward version. (2–3) cannot DELETE/DISABLE/DRAFT program or reward while incomplete members, unexpired program, or PENDING claims exist; **archive** is allowed. (14) **PM-04** reserved lots locked for QR TTL; unclaimed expiry purges lots past `expires_at` (not returned to Available). ([redemption §14](../product/reward-redemption-flow.md#14-decided-pending-edges)) | Backend EXISTS checks + snapshot column + expiry worker |
| Customer OTP limits (PM-06) | **DECIDED:** 3 failed guesses per challenge → 400 `OTP_MAX_ATTEMPTS_EXCEEDED`; 60s resend cooldown; 5 sends per phone per 24h rolling → 429 `DAILY_OTP_LIMIT_REACHED`; TTL **180s**. Phone-keyed Redis, independent of IP ([ADR-012](decisions/ADR-012-public-enrollment-rate-limiting.md) IP limits may coexist). UI timers from `retry_after_seconds`. ([program-model.md](../product/program-model.md#pm-06--customer-otp-security-limits)) | Nest `/auth/otp/send` + `/auth/otp/verify` |
| Campaign automations (PM-18 / DG-10) | **DECIDED Product MVP (Ship 1) Hidden:** no Scheduled Automations UI; writes → 503 `AUTOMATIONS_NOT_AVAILABLE_PHASE1` (or 404). G-09 send/opens remain deferred. ([campaigns-page.md](../frontend/campaigns-page.md#pm-18--hide-scheduled-automations-product-mvp-ship-1)) | Worker in a later release; ADR-013 still owns runtime outside Next |
| New-phone profile fields (UX-75) | **DECIDED Strict:** after new-phone OTP, `full_name`, `email`, `birth_date` required. Enroll 400 `ENROLL_VALIDATION_FAILED`. No merchant optional override. ([program-model.md](../product/program-model.md#ux-75--new-phone-onboarding-required-fields)) | Enroll schema + join form |
| Merchant currency (DG-09 / UX-23) | **DECIDED:** `profiles.currency` is **display metadata** (symbol/label). Changing it does not convert historical `*_cents`, points, or vouchers. Ledger/orders snapshot `currency_code` at write time. Points earn rate is a separate program rule. | Settings + analytics formatters |
| Account active/inactive + filters | **DECIDED:** `admin` sets `staff`/`customer` `active`\|`inactive`. **One page, two tabs:** Team (`admin` + `staff`) and Customers (`customer`). Both tabs: active/inactive + email, name, phone. Team tab also filters role. `admin` rows listed on Team; toggling another `admin` still out ([G-36](../frontend/gaps-and-solutions.md#g-36--no-admin-account-list-or-activeinactive-for-staffcustomer)) | Account list API + login gate |
| Referral rewards (both parties) | **DECIDED:** OTP (SMS/WhatsApp) before any member row; referred granted at verified enroll; referrer granted **only** on first **paid** invoice (`Invoice.Paid`); discount → `vouchers` (`active`/`used`/`expired`); `CHECK (referrer_id <> referred_id)` and `UNIQUE (referred_id)`; same device/IP in the same minute → `pending_review`; share via personal **link** or **QR** on that Shop’s **customer wallet card**; **points** credit the wallet with `expires_at` ([loyalty-page.md](../frontend/loyalty-page.md#referral-rewards-decided), [OTP](../frontend/loyalty-page.md#otp-verification-decided), [fraud](../frontend/loyalty-page.md#referral-fraud-controls-decided), [G-14](../frontend/gaps-and-solutions.md#g-14--referrals-settings-without-attribution)) | Backend Phase 6; do not add tables from this frontend repo |
| Rollback owner / prod acceptance | **ACCEPTED RISK:** not required as a pre-implementation GO gate ([cutover.md](cutover.md))                                                                                                           | Final go-live DNS/webhook activation                                                      |
| Cloudflare Workers path          | Initial host is Vercel; OpenNext/`workerd` remains secondary                                                                                                                                         | Explicit decision to evaluate Workers                                                     |
