# Deferred Decisions

| Decision                         | Why deferred                                                                                                                                                                                         | Revisit trigger                                                                           |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Database redesign                | Would invalidate parity and expand risk; product gaps catalogued as G-01…G-32 ([gaps-and-solutions.md](../frontend/gaps-and-solutions.md)); schema owned by backend ([ADR-014](decisions/ADR-014-product-data-ownership.md)). Engine is PostgreSQL 18.x; NestJS 11.x + Prisma 7.x are **DECIDED** ([ADR-015](decisions/ADR-015-backend-stack.md)). | Backend program executes [remediation-roadmap.md](../backend/remediation-roadmap.md) / [data-contract.md](../backend/data-contract.md) |
| RLS/schema changes               | Existing data contract frozen for Phase 1 frontend migration ([ADR-011](decisions/ADR-011-rls-storage-strategy.md))                                                                                  | Verified security defect with separate approval; or Phase 2 Backend cutover               |
| Microservices/event architecture | No migration requirement                                                                                                                                                                             | Scale or reliability evidence                                                             |
| Payment implementation           | Current billing UI is placeholder (G-07); billing webhook is sole intended writer of `profiles.plan`                                                                                                 | Product approval + provider selection; then Phase 5 of [remediation-roadmap.md](../backend/remediation-roadmap.md) |
| Email delivery provider          | **ACCEPTED RISK:** templates preserved; ship provider-agnostic adapter stubs; no real send until provider chosen                                                                                     | Provider/cost/compliance choice; swap stub for real transport                             |
| SMS provider                     | **ACCEPTED RISK:** channel and message content preserved; stub fails explicitly until configured                                                                                                     | Provider and compliance decision                                                          |
| Internationalization             | Current app is English-only                                                                                                                                                                          | Product localization requirement                                                          |
| Durable job platform             | **Ownership DECIDED** ([ADR-013](decisions/ADR-013-campaign-messaging-runtime.md)): workers outside Next.js; **product still deferred** by workload                                                  | Workload/ops evidence; choose queue vendor without Lovable transport                      |
| Portable observability vendor    | Lovable `window.__lovableEvents` will be removed; replacement vendor not required for architecture                                                                                                   | Production observability needs after Vercel cutover                                       |
| Asset vendoring (Lovable/CDN)    | **DONE (slice 2):** local binaries in `src/assets/`; OG in `public/og-image.png`; manifests removed; `npm run scan:assets` ([10-styling-and-assets.md](../frontend/10-styling-and-assets.md))        | Revisit only if a new marketing asset is introduced via an external CDN                   |
| Vercel env confirm               | **ACCEPTED RISK:** inventory documented ([env.md](../deployment/env.md)); **remember** ΓÇö set/confirm Dev / Preview / Production in Vercel UI before Next deploy; never paste secrets into git/chat | Values confirmed in Vercel UI for the Next project                                        |
| Cookie/SSR session proof (D-28)  | **ACCEPTED RISK:** migration may start; remains **BLOCKED** until authenticated cookie SSR proven ([auth-ssr-spike.md](spikes/auth-ssr-spike.md))                                                    | Service-role or confirmed test user + validate harness PASS                               |
| Differentiated in-app roles      | **Locked names:** `admin` (buyer `/app`), `staff` (`/app`, **same permissions as `admin` for now**), `customer` (customer login). Phase 1 Redemption: any existing Staff or Admin may **scan/verify**, Shop-scoped. Staff subtypes and a later permission split are not locked. ([ADR-005](decisions/ADR-005-authentication.md), [locked role matrix](../frontend/11-authentication-migration.md#locked-role-matrix), [redemption §11](../product/reward-redemption-flow.md#11-staff-authorization-shop-level)) | Product approval to split `staff` permissions; backend owns schema |
| Shop-customer register / login   | **DECIDED as product intent:** shop customers will register/login to store their data and support **calculated KPIs**; not only owner manual add. **Passwordless:** register, login, and recovery never use a password; lost access = new OTP (SMS/WhatsApp). Routes, identity schema, and KPI list **not locked**. Working design journey (2026-08-16): one OTP funnel; first-shop link + profile setup screens; in-store returning check-in stays without OTP ([customer-portal-journey.md](../product/customer-portal-journey.md)). Not `admin` / `staff` `/app` auth ([G-33](../frontend/gaps-and-solutions.md#g-33--shop-customers-have-no-registerlogin-kpis-rely-on-owner-typed-rows), [credential recovery](../frontend/11-authentication-migration.md#credential-recovery-decided)) | Backend program + route map approval; do not add tables from this frontend repo |
| Admin creates admin/staff        | **DECIDED:** `admin` form creates `admin` or `staff`, random temp password, email with added + email + password; **first login must change the password**. After the account exists, forgotten password is the owner `/auth/forgot-password` flow; admin may re-issue a temp password. Route and extra profile fields not locked ([G-34](../frontend/gaps-and-solutions.md#g-34--admin-cannot-create-adminstaff-with-emailed-temp-password), [credential recovery](../frontend/11-authentication-migration.md#credential-recovery-decided)) | Backend create-user + messaging contracts + first-login password gate |
| Multiple loyalty programs        | **DECIDED:** many programs per shop, including **same type** (no 1-per-type). Each `draft` \| `active` \| `disabled`. Join/check-in only when `active`. Membership/points/stamps/wallet/rewards/redemptions stay program-scoped; selecting one Program never auto-joins siblings. Shop is a container (no shop-wide wallet). Conditional earning modifiers (e.g. Happy Hour 3x) are **rules inside one Program**. Create UI must surface rule-vs-program guidance. **PENDING Business Owner:** whether more than one program may be `active` at once, and therefore Shop QR behavior (one-active → `/join/{programId}`; multiple-active → selection then `/join/{programId}`). Creating any number is already decided. Today `UNIQUE (owner_id)` ([G-35](../frontend/gaps-and-solutions.md#g-35--shop-is-limited-to-one-loyalty-program-no-program-status), [program-model.md](../product/program-model.md), [counter QR](../product/counter-qr-and-program-membership.md#15-shop-qr--multiple-active-programs-pending)) | Drop unique; add `status`; Shop QR after BO decision |
| Customer wallet (per program) | **DECIDED:** points never mix across programs; one card per membership of any type mix (Points: numeric available + next-reward progress; Visit: stamp icons; Tier: current + next threshold). Spendable / **available** = total − pending reserved + expiry groups + vouchers + share. Portal URL not locked ([program-model.md](../product/program-model.md#4-customer-wallet-summary), [loyalty-page.md](../frontend/loyalty-page.md#customer-wallet-per-program-decided), [customer-reward-progress.md](../product/customer-reward-progress.md), [G-33](../frontend/gaps-and-solutions.md#g-33--shop-customers-have-no-registerlogin-kpis-rely-on-owner-typed-rows)) | Backend customer session + `GET /api/me/wallet`; do not add tables from this frontend repo |
| Tier downgrade if activity drops | **Not decided.** Tier is status (standing perks), not spendable. Whether a member downgrades when activity drops — and how today’s `tierDowngradeProtection` flag behaves — has no product default. Do not invent one. ([program-model.md](../product/program-model.md#open--tier-downgrade)) | Product Owner choice before implementing downgrade / protection |
| Signup Bonus vs Referral Bonus | **DECIDED:** both Program-scoped. Signup: once per customer per Program on first membership join, regardless of referral. Referral: requires valid `?ref=` + OTP; can **stack** with Signup if both configured. Welcome / link-program screens are UX only (no implied bonus). Referral **referrer** grant still waits for first paid invoice. ([Signup vs Referral](../frontend/loyalty-page.md#signup-bonus-vs-referral-bonus-decided)) | Backend enroll + ledger `signup_bonus`; do not add tables from this frontend repo |
| Catalog redemption lifecycle | **DECIDED (Phase 1):** customer Redeem → if Available < cost, error (no row); else `pending` (reserve + single-use QR, 10-minute TTL) → staff **scan** `completed` (atomic `UPDATE … WHERE status = 'pending'`, affected rows = 1) or job `expired` (release Reserved). Scan is verification, not approval; staff cannot reject a valid QR. Create idempotent; second scan → “already redeemed”; expired QR → “expired”. Earn idempotent per business event; concurrent earn+redeem same consistency model; program isolation; Shop-level staff scan authz; Phase 1 any Staff/Admin role; reward eligibility at create (later reward `expires_at` does not auto-invalidate; QR TTL independent). Digital catalog rewards may complete instantly. **Do not implement** staff Approve/Reject for physical rewards (superseded). **Deferred:** refund / reversal (not Phase 1). ([reward-redemption-flow.md](../product/reward-redemption-flow.md), [G-20](../frontend/gaps-and-solutions.md#g-20--rewardsredeemed_count-vs-earn)) | Backend redeem APIs + expiry worker; do not add tables from this frontend repo |
| Pending redemption vs later disable / price / expiry | **Pending Product Owner — do not implement:** (1) reward price change while PENDING; (2) reward disabled/deleted while PENDING; (3) program disabled while PENDING redemptions exist; (14) point-lot expiry while points are reserved. No recommended default. ([redemption §14](../product/reward-redemption-flow.md#14-pending-owner-decisions-do-not-implement-yet)) | Product Owner choice before implementing those paths |
| Shop QR / multiple ACTIVE programs | **Pending Business Owner — do not finalize Shop QR:** whether a Shop may have multiple ACTIVE programs at once. If one: Shop QR → `/join/{programId}`. If many: Shop QR → selection → `/join/{programId}`. Selecting one never auto-joins others. ([counter QR §15](../product/counter-qr-and-program-membership.md#15-shop-qr--multiple-active-programs-pending)) | Business Owner decision |
| Account active/inactive + filters | **DECIDED:** `admin` sets `staff`/`customer` `active`\|`inactive`. **One page, two tabs:** Team (`admin` + `staff`) and Customers (`customer`). Both tabs: active/inactive + email, name, phone. Team tab also filters role. `admin` rows listed on Team; toggling another `admin` still out ([G-36](../frontend/gaps-and-solutions.md#g-36--no-admin-account-list-or-activeinactive-for-staffcustomer)) | Account list API + login gate |
| Referral rewards (both parties) | **DECIDED:** OTP (SMS/WhatsApp) before any member row; referred granted at verified enroll; referrer granted **only** on first **paid** invoice (`Invoice.Paid`); discount → `vouchers` (`active`/`used`/`expired`); `CHECK (referrer_id <> referred_id)` and `UNIQUE (referred_id)`; same device/IP in the same minute → `pending_review`; share via personal **link** or **QR** on that program’s **customer wallet card**; **points** credit the wallet with `expires_at` ([loyalty-page.md](../frontend/loyalty-page.md#referral-rewards-decided), [OTP](../frontend/loyalty-page.md#otp-verification-decided), [fraud](../frontend/loyalty-page.md#referral-fraud-controls-decided), [G-14](../frontend/gaps-and-solutions.md#g-14--referrals-settings-without-attribution)) | Backend Phase 6; do not add tables from this frontend repo |
| Rollback owner / prod acceptance | **ACCEPTED RISK:** not required as a pre-implementation GO gate ([cutover.md](cutover.md))                                                                                                           | Final go-live DNS/webhook activation                                                      |
| Cloudflare Workers path          | Initial host is Vercel; OpenNext/`workerd` remains secondary                                                                                                                                         | Explicit decision to evaluate Workers                                                     |
