/**
 * Transactional HTML/text builders will be extracted from
 * `join.functions.ts` / `notifications.functions.ts` / `security.functions.ts`
 * when those domains migrate. Keep this folder as the target home (ADR-007).
 */
export {};
